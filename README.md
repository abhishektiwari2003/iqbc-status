# IQBC service status

Public out-of-band status channel for the IQBC-Test app. The app reads `status.json` from this repo (GitHub raw) to show maintenance/outage notices even when the backend is down. Updated automatically by the status monitor workflow and by the super-admin console.
